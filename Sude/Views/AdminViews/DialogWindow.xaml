<Window x:Class="Sude.Views.AdminViews.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sude.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" SizeToContent="WidthAndHeight" WindowStyle="SingleBorderWindow" 
        AllowsTransparency="False" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <materialDesign:Card UniformCornerRadius="15">
        <materialDesign:Card.Effect>
            <DropShadowEffect Color="#BB2248" BlurRadius="20" Opacity="0.3" ShadowDepth="2"/>
        </materialDesign:Card.Effect>
        <Grid>
            <ContentControl Content="{Binding CurrentPage}"/>
        </Grid>
    </materialDesign:Card>
</Window>
 